from setuptools import setup
setup(name="Gestion Blog", 
      version="0,1",
      description="Soporte en la gestión de blogs",
      author='mgimenez',
      packages = ['gestionblog']
      )